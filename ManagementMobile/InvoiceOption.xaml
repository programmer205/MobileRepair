<UserControl x:Class="ManagementMobile.InvoiceOption"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ManagementMobile"
             mc:Ignorable="d" 
             Visibility="Hidden"
             d:DesignHeight="40" d:DesignWidth="400">
    <Grid Margin="0,10,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="txt_code" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="#D4A373" FontFamily="IRANSansWeb(FaNum)" TextAlignment="Center"/>
        <TextBlock x:Name="txt_model" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="#D4A373" FontFamily="IRANSansWeb(FaNum)" TextAlignment="Center"/>
        <TextBlock x:Name="txt_price" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Foreground="#D4A373" FontFamily="IRANSansWeb(FaNum)" TextAlignment="Center"/>
    </Grid>
</UserControl>
